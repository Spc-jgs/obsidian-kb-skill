"""Command-line tools for the Obsidian KB skill.

Exposes three importable modules whose ``main()`` functions also back the
console-script entry points:

* ``audit_vault`` — read-only vault auditor
* ``process_inbox`` — propose/apply filing of Inbox quick-capture notes
* ``suggest_links`` — read-only wikilink target suggestions for one note
"""

"""obsidian_kb_skill — packaged CLI tools and bundled resources.

The agent-facing skill body lives in core/ (and is built into platform adapters
by build.py). The deterministic, testable command-line tools live under
obsidian_kb_skill.scripts and are exposed as console_scripts. Bundled templates
and references ship as package data under obsidian_kb_skill.scripts.resources.
"""

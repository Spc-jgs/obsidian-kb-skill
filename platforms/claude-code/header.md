# Obsidian Personal Knowledge Base

**When to use**: On explicit save intent ("save to Obsidian", "沉淀", "记一下") or an explicit request to evaluate what from a conversation is worth capturing. Harvest analysis may be read-only; never mutate the Vault without save intent. Never use for unrelated Q&A/debug/chat.

**Installed resources**: Resolve `<skill-root>` as `~/.obsidian-kb-skill/skill` for references, assets, and bundled helpers.

"""Bundled runtime resources for the Obsidian KB skill.

This subpackage is shipped as package data inside the wheel. Its contents
(templates/, references/) are the canonical resources an installed
``obsidian-kb-skill`` console script reads. Users edit templates inside their
vault, NOT here — this is the shipped starter set only.
"""
